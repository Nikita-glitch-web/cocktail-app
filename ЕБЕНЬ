Користувач робить запит на сервер який повертає йому html сторінку з певними стилямя

Браузер формирует HTTP-запрос, который состоит из строки запроса, тела сообщения и заголовков со служебной информацией, 
такой как версия протокола1.

Браузер отправляет запрос интернет-провайдеру (ISP), чтобы узнать, что такое "google.com"2.

ISP в свою очередь отправляет запрос в DNS (Domain Name Service), чтобы получить IP-адрес для "google.com"3.

DNS возвращает IP-адрес браузеру.

Браузер отправляет запрос на полученный IP-адрес.

Сервер Google обрабатывает запрос и возвращает веб-страницу с результатами поиска или другой контент, связанный с "google.com"1.